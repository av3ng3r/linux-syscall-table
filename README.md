# linux-syscall-table
This repository contains a Python script that generates a comprehensive Linux system call table in Markdown format. The table includes the syscall number, name, brief description, and the function prototype extracted dynamically from the system's man pages. The generated Markdown file is suitable for documentation, learning, or reference purposes.
